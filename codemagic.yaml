workflows:

  ios-build:
    name: iOS Build
    max_build_duration: 120
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - flutter pub get
      - cd ios && pod install
      - flutter build ios --release --no-codesign
    artifacts:
      - build/ios/ipa/*.ipa
